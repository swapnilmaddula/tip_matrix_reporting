# This is a Databricks asset bundle definition for tip_challenge.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: tip_challenge
  uuid: d9dde8a6-a6a7-4685-b8ba-6ccc4030b5d8

include:
  - resources/*.yml

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://adb-1739243129273354.14.azuredatabricks.net

  prod:
    mode: production
    workspace:
      host: https://adb-1739243129273354.14.azuredatabricks.net
      # We explicitly deploy to /Workspace/Users/swapnilmaddula@gmx.co.uk to make sure we only have a single copy.
      root_path: /Workspace/Users/swapnilmaddula@gmx.co.uk/.bundle/${bundle.name}/${bundle.target}
    permissions:
      - user_name: swapnilmaddula@gmx.co.uk
        level: CAN_MANAGE
